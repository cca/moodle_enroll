[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
openpyxl = "==3.1.5"
click = "==8.1.7"

[dev-packages]
pytest = "~=8.3"

[requires]
python_version = "3.11"

[scripts]
clean = "find . -name '*.csv' -type f -delete"
test = "pytest -v enroll"
